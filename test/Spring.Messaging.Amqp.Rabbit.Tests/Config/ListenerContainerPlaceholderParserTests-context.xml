<?xml version="1.0" encoding="UTF-8"?>
<objects xmlns="http://www.springframework.net"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns:rabbit="http://www.springframework.net/schema/rabbit"
         xsi:schemaLocation="http://www.springframework.net/schema/rabbit http://www.springframework.net/schema/rabbit/spring-rabbit-1.0.xsd">

	<bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="properties">
			<props>
				<prop key="five">5</prop>
				<prop key="one">1</prop>
				<prop key="false">false</prop>
			</props>
		</property>
	</bean>

	<rabbit:queue name="foo" />

	<rabbit:queue id="bar" />

	<rabbit:listener-container id="container1" connection-factory="ConnectionFactory" acknowledge="manual" concurrency="${five}" transaction-size="${one}" auto-startup="${false}">
		<rabbit:listener id="testListener" queue-names="foo, #{bar.name}" ref="TestObject" method="handle"/>
	</rabbit:listener-container>

	<object id="ConnectionFactory" class="Spring.Messaging.Amqp.Rabbit.Connection.CachingConnectionFactory"/>

	<object id="TestObject" class="org.springframework.amqp.rabbit.config.ListenerContainerParserTests$TestBean"/>

</objects>
